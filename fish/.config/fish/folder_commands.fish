# Shortcut commands for commonly accessed folders
alias phys cd
complete -c phys -a '(__fish_complete_directories ~/Physics/)'

alias lbd cd
complete -c lbd -a '(__fish_complete_directories ~/Documents/LHCb/)'

alias code cd
complete -c code -a '(__fish_complete_directories ~/Code/)'
